# Edit Patch 7（Orchestrator 补发 · 裁决 #3）

- 来源：Validator 判定 4 条 D 类（修改未落盘，均为 Step 4 分发/执行遗漏），依执行约束「回退机制必须生效」之反面——遗漏项须补齐而非回退；内容严格取自 `_temp/edit-instructions.md` 修改建议原文
- 覆盖指令：#2（L154 部分）、#51、#52、#54
- 锚点行号均指当前修订稿 `prompt-revised-draft.md`（403 行）

## Patch #2-L166
- 类型：替换
- 锚点行（修订稿 L166，完整一行）：| Writer Agent(s) | 具体文档的创建/改写 | 仅目标文档相关的已有内容 + 约束段落 + `_temp/verify-*.md` 结果 |
- 新行：| Writer Agent(s) | 具体文档的创建/改写 | 仅目标文档相关的已有内容 + 约束段落 + `_temp/verify-*.md` 与 `_temp/infer-*.md` 结果 |
- 依据：审查报告 #2（指令 #2 的第三处替换，原分发遗漏）

## Patch #51
- 类型：插入
- 锚点行（修订稿 L93，完整一行）：**偏差处理：** 发现不一致时，以实际代码/配置为准，并在输出中明确标注 `[存在差异]`，同时记录差异详情至 `_temp/verify-discrepancies.md`。
- 新行（共插入 1 行，紧跟锚点行之后、L94 空行之前）：若实际产物之间相互矛盾，以运行时实际生效者为准；无法判定生效关系时，将矛盾各方均标注 `[存在差异]`，矛盾对记入 `_temp/verify-discrepancies.md` 并列入待人工确认清单。
- 依据：审查报告 #51

## Patch #52
- 类型：插入
- 锚点行（修订稿 L223，完整一行）：**冲突处理：** `README.md` 与 `AGENTS.md` 或 `docs/` 冲突时，以 `AGENTS.md` 为最终权威，按校验机制修正。
- 新行（共插入 1 行，紧跟锚点行之后、L224「完整优先级链」行之前）：docs/ 同级文档之间冲突时，一律以 AGENTS.md 表述为准；AGENTS.md 无表述的，冲突双方均标注 `[存在差异]` 并将矛盾对列入汇报等待用户裁定，不得擅自删除任何一方。
- 依据：审查报告 #52（锚点为原基底 L207「冲突处理」行；patch-3 #47 已在原 L207 之后插入过一行，本条按锚点文本唯一定位，紧随「冲突处理」行）

## Patch #54
- 类型：插入
- 锚点行（修订稿 L390，完整一行）：6. **最终校验：** 操作完成后立即执行一次完整全局校验
- 新行（共插入 1 行，紧跟锚点行之后、L391「7. 输出汇报」之前，不带缩进）：最终校验不通过的，按第五节修正后重跑；达到上一条 3 轮上限仍不通过的，允许带着未通过清单结束任务，但须在汇报中置顶说明并标注 `[待补充]`。
- 依据：审查报告 #54（插入后位于 #38 替换行与 #50 插入的 Git 边界行之后，归属第六节条目 6 的补充说明）
