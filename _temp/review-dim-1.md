# 维度 4.1 审查结果：术语一致性

被审查文件：`d:\Personal-PitFall-Log\prompts\Project-Spec-build.md`（共 381 行）
严重度图例：❌ 不明确 ｜ ⚠️ 有歧义 ｜ ✅ 达标

---

## ① 同一概念全文同名

### 问题 1｜❌ 不明确｜「情况/模式 A/B/C/D」字母三套体系复用，含义直接冲突

- 行号+原文摘录：
  - L32：`### 0.2 执行模式判定`；L38–L41：`A. 从零新建 / B. 局部补全 / C. 重构规范化 / D. 校验修正`
  - L141：`### 模式 A：多 Agent 协作（优先）`；L166：`### 模式 B：单 Agent（降级）`
  - L261–L262：`| A | 单 Agent 直接执行 | … |`、`| B | 多 Agent 协作执行 | … |`
- 问题描述：同一字母组合被用于三套不同分类体系。其中 L141「模式 A」= 多 Agent 协作，而 L261「模式 A」= 单 Agent 直接执行；L166「模式 B」= 单 Agent（降级），而 L262「模式 B」= 多 Agent 协作执行——同名且含义相反，执行者无法确定任意一处「模式 A/B」所指。
- 具体修改建议：
  - 将第 32 行『### 0.2 执行模式判定』改为『### 0.2 构建路径判定』；
  - 将第 141 行『### 模式 A：多 Agent 协作（优先）』改为『### 构建协作方式一：多 Agent 协作（优先）』；
  - 将第 166 行『### 模式 B：单 Agent（降级）』改为『### 构建协作方式二：单 Agent（降级）』；
  - 「模式 A/B/C/D」字样保留给 L259–L264 中 `docs/agent-execution-mode.md` 专用，全文其他位置不再复用。

### 问题 2｜⚠️ 有歧义｜「执行模式/协作模式/执行路径/运行方式」交叉混用

- 行号+原文摘录：
  - L32：`执行模式判定`（指 A/B/C/D 情况）
  - L62：`执行路径：简述将做什么`；L352：`执行路径：新建 / 局部补全 / 重构规范化 / 校验修正`（同一事物 L32 叫「执行模式」、L352 叫「执行路径」）
  - L63：`协作模式：多 Agent / 单 Agent`；L351：`执行模式：多Agent协作 / 单Agent降级`（此处「执行模式」又改指协作形态，与 L32 撞义）
  - L359：`模式判定`
- 问题描述：同一事物在不同位置换用不同名称，且「执行模式」一词同时承担两种含义（构建路径分类、协作形态分类），读者无法靠术语本身定位所指。
- 具体修改建议：
  - 将第 351 行『执行模式：多Agent协作 / 单Agent降级』改为『协作模式：多 Agent 协作 / 单 Agent 降级』；
  - 将第 359 行『（现状扫描 → 模式判定 → 协作检测 → 决策报告）』改为『（现状扫描 → 构建路径判定 → 协作检测 → 决策报告）』。

### 问题 3｜⚠️ 有歧义｜「推测」与「推断」作为总称混用

- 行号+原文摘录：
  - L72：`## 一、规约真实性校验与自适应推测（核心增量逻辑）`（总称用「推测」）
  - L74：`都必须在受限范围内进行自适应推断`（总称改用「推断」）
  - L131：`[推测]｜无直接文档，基于客观线索推断`（`[推测]` 标签的定义句使用「推断」一词）
- 问题描述：`[推测]` 与 `[推断]`（L127–L132）是两个并列的独立标签，但正文又用「推测/推断」互替地充当总称，且 `[推测]` 的定义借助「推断」表述，导致两个标签的边界与总称含义互相缠绕、无法区分。
- 具体修改建议：
  - 将第 74 行『都必须在受限范围内进行自适应推断』改为『都必须在受限范围内进行自适应推测』；
  - 将第 131 行『无直接文档，基于客观线索推断』改为『无直接文档，基于单一客观线索推测得出』；
  - 将第 132 行『基于多处间接证据综合得出』改为『基于多处间接证据综合推断得出（线索数量 ≥ 2 且相互印证）』。

### 问题 4｜⚠️ 有歧义｜同一角色异名：「主 Agent」vs「Orchestrator（主控）」

- 行号+原文摘录：
  - L120：`主 Agent 仅读取 _temp/ 下的结果文件进行汇总`
  - L118：`Orchestrator 将校验/推测任务按模块/目录拆分为独立子任务`；L151：`Orchestrator（主控）｜任务拆解、分发、汇总、校验调度`
- 问题描述：「汇总」职责在 L120 归于「主 Agent」，在 L151 归于「Orchestrator（主控）」，同一角色两个名字，无法确认是否为同一实体。
- 具体修改建议：将第 120 行『主 Agent 仅读取』改为『Orchestrator（主控）仅读取』。

### 问题 5｜⚠️ 有歧义｜「规约」与「规范」在「应遵守的要求」义项上重叠

- 行号+原文摘录：
  - L3：`具备一套符合下述规范的 **Agent 规约**`
  - L38：`按规范完整创建`；L40：`不符合本规范`；L41：`基本符合规范`
  - （对照组：交付物全程称「规约」：L3「Agent 规约」、L38「规约体系」、L179「规约体系标准」）
- 问题描述：「规约」稳定指交付物体系；但「规范」在 L3/L38/L40/L41 指本提示词标准、在 L114/L139/L200/L227/L234 又泛指规则文档，两词在「要求」义上重叠，L38「按规范完整创建」可能被误读为另建一份名为「规范」的产物。
- 具体修改建议：
  - 将第 3 行『符合下述规范的』改为『符合本提示词下述标准的』；
  - 将第 38 行『按规范完整创建』改为『按本提示词完整创建规约』；
  - 将第 40 行『不符合本规范』改为『不符合本提示词标准』；
  - 将第 41 行『基本符合规范』改为『基本符合本提示词标准』。
- 附注（✅ 子项）：「约定」仅 L203「架构约定」作为文档示例出现、未与「规约」混用；「审查」全文 0 次、「审计」仅 L316/L325 用于 Step 1 且与「校验」为包含关系，均不构成混用。

## ② 文件路径全文一致

### 问题 6｜✅ 达标｜路径大小写全文一致

- 核查方式：对 `AGENTS.md`、`INDEX.md`、`agent-execution-mode.md` 做不区分大小写全文检索（覆盖 L26–L374 全部出现点），未发现小写或大小写混写变体。

### 问题 7｜⚠️ 有歧义｜`docs/agent-execution-mode.md` 存在省略 `docs/` 前缀的写法

- 行号+原文摘录：
  - L242：`` `agent-execution-mode.md` 必须作为索引首条或高优先级条目 ``
  - L332：`` **`agent-execution-mode.md` 已列入索引且路径正确** ``
  - L348：`agent-execution-mode.md 已收录`
  - L365：`**含 `agent-execution-mode.md`**`
  - （其余 10 处均为完整的 `docs/agent-execution-mode.md`；L192 目录树内的相对写法可接受）
- 问题描述：同一文件 4 处缺前缀、10 处带前缀，写法不统一，Agent 在索引校验（Step 2）时可能按无路径文件名匹配而漏检。
- 具体修改建议：
  - 将第 242 行『`agent-execution-mode.md` 必须作为索引首条』改为『`docs/agent-execution-mode.md` 必须作为索引首条』；
  - 将第 332 行『`agent-execution-mode.md` 已列入索引且路径正确』改为『`docs/agent-execution-mode.md` 已列入索引且路径正确』；
  - 将第 348 行『agent-execution-mode.md 已收录』改为『docs/agent-execution-mode.md 已收录』；
  - 将第 365 行『**含 `agent-execution-mode.md`**』改为『**含 `docs/agent-execution-mode.md`**』。

### 问题 8｜⚠️ 有歧义｜L365 `INDEX.md` 缺 `docs/` 前缀

- 行号+原文摘录：L365：`创建/更新 INDEX.md`（其余 L155、L201、L224、L321、L329、L348 均为 `docs/INDEX.md`）
- 问题描述：同一文件仅此处省略前缀，与全文写法不一致。
- 具体修改建议：将第 365 行『创建/更新 `INDEX.md`』改为『创建/更新 `docs/INDEX.md`』。

### 问题 9｜❌ 不明确｜`_temp/verify-*.md` 与 `infer-*.md` 命名模式自相矛盾

- 行号+原文摘录：
  - L119：`每个子 Agent 将结果写入 _temp/verify-*.md（如 _temp/verify-backend-deps.md、_temp/infer-tech-stack.md）`
  - L154：Writer Agent 加载上下文为 `_temp/verify-*.md 结果`
  - L162：`详细校验/推测结果写入 _temp/verify-*.md`
  - 对照组 L188–L189：`verify-*.md # 一致性校验结果` / `infer-*.md # 自适应推测结果`；L362：`将结果写入 _temp/verify-*.md 和 _temp/infer-*.md`
- 问题描述：L119 声明的模式是 `verify-*.md` 但给出的示例却含 `infer-tech-stack.md`（不匹配该模式）；L162 要求把「推测」结果也写入 `verify-*.md`，与 L189/L362 的 `infer-*.md` 归属直接冲突；L154 只列 `verify-*.md` 又遗漏推测结果文件。执行者无法确定推测结果应命何名。
- 具体修改建议：
  - 将第 119 行『将结果写入 `_temp/verify-*.md`』改为『将结果写入 `_temp/verify-*.md`（校验类）或 `_temp/infer-*.md`（推测类）』；
  - 将第 162 行『详细校验/推测结果写入 `_temp/verify-*.md`』改为『详细校验结果写入 `_temp/verify-*.md`，详细推测结果写入 `_temp/infer-*.md`』；
  - 将第 154 行『`_temp/verify-*.md` 结果』改为『`_temp/verify-*.md` 与 `_temp/infer-*.md` 结果』。

## ③ 缩写/专有名词首次出现有定义

### 问题 10｜⚠️ 有歧义｜Orchestrator 首次出现（L118）无定义

- 行号+原文摘录：L118：`Orchestrator 将校验/推测任务按模块/目录拆分为独立子任务`；释义「（主控）」迟至 L151 才出现。
- 问题描述：专有名词首次出现未带定义，读者在 L118–L150 区间无法确认其角色。
- 具体修改建议：将第 118 行『Orchestrator 将校验/推测任务』改为『Orchestrator（主控）将校验/推测任务』。

### 问题 11｜⚠️ 有歧义｜MCP、CI/CD 首次出现未给出定义

- 行号+原文摘录：L11：`若项目依赖 MCP 服务或特定 Skills（如 Git 操作等）`；L80：`CI/CD 配置`
- 问题描述：两处缩写首次出现均未展开，属于非通用度低于 Git/README 的缩写，存在理解门槛。
- 具体修改建议：
  - 将第 11 行『依赖 MCP 服务』改为『依赖 MCP（Model Context Protocol）服务』；
  - 将第 80 行『CI/CD 配置』改为『CI/CD（持续集成/持续部署）配置』。

### 问题 12｜⚠️ 有歧义｜多 Agent 机制名称清单两处写法不一致

- 行号+原文摘录：L12：`子 Agent 派发 / Worker / Task Delegation 等`；L279：`检测是否存在 Task Delegation / Worker / Sub-Agent 等机制`
- 问题描述：同类机制在两处顺序与措辞不同（中文「子 Agent 派发」vs 英文「Sub-Agent」），易被误判为两套不同的检测标准。
- 具体修改建议：将第 279 行『检测是否存在 Task Delegation / Worker / Sub-Agent 等机制』改为『检测是否存在子 Agent 派发（Task Delegation / Worker / Sub-Agent）等机制』，与第 12 行保持一致。

---

## 维度 4.1 总判定

**不达标（需修改）**：共发现 ❌ 2 项（「模式/情况 A/B/C/D」字母三套体系复用且含义相反；`_temp/verify-*.md` 与 `infer-*.md` 命名模式自相矛盾），⚠️ 9 项（执行模式/协作模式/执行路径混用、推测/推断总称混用、主 Agent/Orchestrator 异名、规约/规范重叠、`docs/` 前缀省略 2 处、Orchestrator 首现无定义、MCP/CI/CD 未定义、机制名称清单不一致），✅ 2 项（路径大小写一致、「约定/审查」无混用）。两项 ❌ 会直接误导执行行为，须优先修复后方可进入下一环节。
