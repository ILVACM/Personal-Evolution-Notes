# Edit Patch — Editor Agent 3（原文 L197–L252 区间）

行号说明：指令库行号与实际文件行号存在统一 +1 偏移（指令库 L207=实际 L206、指令库 L239=实际 L238、指令库 L242=实际 L241、指令库 L248=实际 L247、指令库 L251=实际 L250）。以下"原行号"均以实际文件为准，并在括号内注明指令库行号；Merger 请优先以"原文行"文本做精确匹配。

---

## Patch #8
- 类型：替换
- 原行号：L247（指令库 L248）
- 原文行（完整一行，供 Merger 精确匹配）：`- 根据项目现状组织具体规约文档（架构说明、编码规范、开发流程、环境配置等）`
- 新行（替换为完整新行）：`- 依据 `_temp/` 校验与推测结果中存在的线索组织具体规约文档：`agent-execution-mode.md` 为必建；其余文档（架构说明、编码规范、开发流程、环境配置等）仅在有实际线索支撑时创建，无线索不建、不得以空模板充数`
- 依据：审查报告 #8

---

## Patch #15+#31（按 Orchestrator 裁决 1 合并为一条替换）
- 类型：替换
- 原行号：L241（指令库 L242）
- 原文行（完整一行，供 Merger 精确匹配）：`- `agent-execution-mode.md` 必须作为索引首条或高优先级条目`
- 新行（替换为完整新行）：`- `docs/agent-execution-mode.md` 必须作为索引清单第 1 条；任何顺序调整不得使其排在第 3 条之后。`
- 依据：审查报告 #15、#31

---

## Patch #32（按 Orchestrator 裁决 2 采用 #32 主建议句）
- 类型：替换
- 原行号：L250（指令库 L251）
- 原文行（完整一行，供 Merger 精确匹配）：`- 已有文档应被合理归入此目录（移动/重命名），而非丢弃`
- 新行（替换为完整新行）：`- 已有文档必须被合理归入此目录（移动/重命名），不得丢弃`
- 依据：审查报告 #32

---

## Patch #58+#60（按 Orchestrator 裁决 2 合并为一个插入，先 #58 后 #60）
- 类型：插入
- 原行号：L250（指令库 L251；锚点行与 Patch #32 为同一行，新内容加在该行之后；请在 Patch #32 替换之后应用）
- 原文行（锚点行，供 Merger 精确匹配）：`- 已有文档应被合理归入此目录（移动/重命名），而非丢弃`
- 新行（插入两行，逐行列出）：
  1. `- 按惯例保留在根目录的文档（LICENSE、CHANGELOG、CONTRIBUTING 等）可原地保留、不纳入 `docs/INDEX.md`，但须在决策报告『现状』中列明。`
  2. `- docs/ 下的非规约类文件（图片、设计稿、会议纪要等）原地保留或移入 `docs/assets/` 等子目录，不纳入 INDEX.md，并在汇报中说明处理方式。`
- 依据：审查报告 #58、#60

---

## Patch #46（第二部分：4.2 节插入；树状图部分已由 Editor 2 处理，此处不重复）
- 类型：插入
- 原行号：L238（指令库 L239；锚点为 4.2 节标题行，新内容加在该行之后）
- 原文行（锚点行，供 Merger 精确匹配）：`### 4.2 `docs/INDEX.md``
- 新行（插入一行）：`**此文档为规约体系的必建交付物，构建时必须生成，不可省略。**`
- 依据：审查报告 #46

---

## Patch #47（按 Orchestrator 裁决 4）
- 类型：插入
- 原行号：L206（指令库 L207；锚点为含『以 `AGENTS.md` 为最终权威，按校验机制修正。』的行，新内容加在该行之后）
- 原文行（锚点行，供 Merger 精确匹配）：`**冲突处理：** `README.md` 与 `AGENTS.md` 或 `docs/` 冲突时，以 `AGENTS.md` 为最终权威，按校验机制修正。`
- 新行（插入一行）：`完整优先级链：本提示词 > `AGENTS.md` > `docs/agent-execution-mode.md` / `docs/INDEX.md` > 其他 `docs/*.md` > `README.md`；任意两级冲突时以高一级为准并按第五节修正；任何文档与项目实际状态冲突时，以实际状态为准（见 1.1 节）。`
- 依据：审查报告 #47
