# Editor Agent 4 补丁文件（原文 L253–L381）

说明：
- 共 23 个 Patch，按原行号升序排列；新行文本均直接取自指令建议原文，保留原行缩进、列表层级与格式。
- 超出本区间（L253–L381）未出 Patch 的指派位置：#2 的 L119/L154/L162/L189；#11 的 L32/L62–L63（L359 依裁决归 #6）；#15 的 L242；#19 的 L12；#29 的 L147；#42 的 L312；#53 的 L335（其上限语义已并入 L325 后的插入行）。
- L279 的替换依 Orchestrator 裁决以 #29 编号执行，新行文本取自指令 #19 的建议原文。

## Patch #34
- 类型：替换
- 原行号：L261
- 原文行（完整一行，供 Merger 精确匹配）：| A | 单 Agent 直接执行 | 任务范围小、上下文可控、无需拆分 | 一个 Agent 顺序完成全部操作 |
- 新行（替换为完整新行；插入可多行，逐行列出）：| A | 单 Agent 直接执行 | 任务范围小（参考阈值：涉及文件 ≤5 个且模块 ≤2 个，可按项目调整）、上下文可控、无需拆分 | 一个 Agent 顺序完成全部操作 |
- 依据：审查报告 #34

## Patch #34
- 类型：替换
- 原行号：L262
- 原文行（完整一行，供 Merger 精确匹配）：| B | 多 Agent 协作执行 | 任务复杂、涉及多模块/多文件、环境支持子 Agent 派发 | Orchestrator 拆分任务，多 Agent 并行/串行协作 |
- 新行（替换为完整新行；插入可多行，逐行列出）：| B | 多 Agent 协作执行 | 任务复杂（参考阈值：涉及文件 >5 个或模块 >2 个）、涉及多模块/多文件、环境支持子 Agent 派发 | Orchestrator 拆分任务，多 Agent 并行/串行协作 |
- 依据：审查报告 #34

## Patch #34
- 类型：替换
- 原行号：L264
- 原文行（完整一行，供 Merger 精确匹配）：| D | 混合模式 | 部分环节可并行、部分必须串行；或环境仅支持有限协作 | 按环节混合使用，明确标注哪些步骤并行、哪些串行 |
- 新行（替换为完整新行；插入可多行，逐行列出）：| D | 混合模式 | 环境仅支持部分协作能力（如可派发子 Agent 但不支持并行），需按环节混合串并行，且与模式 B 互斥：环境完整支持协作时不得选用本模式 | 按环节混合使用，明确标注哪些步骤并行、哪些串行 |
- 依据：审查报告 #34

## Patch #33
- 类型：替换
- 原行号：L277
- 原文行（完整一行，供 Merger 精确匹配）：提供具体的检测指令，例如：
- 新行（替换为完整新行；插入可多行，逐行列出）：必须提供至少一条可直接执行的检测步骤，每条步骤须包含：执行动作、成功的表现、失败的表现（下方示例可直接采用或等价替换：）
- 依据：审查报告 #33

## Patch #29
- 类型：替换
- 原行号：L279
- 原文行（完整一行，供 Merger 精确匹配）：- 检测是否存在 Task Delegation / Worker / Sub-Agent 等机制
- 新行（替换为完整新行；插入可多行，逐行列出）：- 检测是否存在子 Agent 派发（Task Delegation / Worker / Sub-Agent）等机制
- 依据：审查报告 #19（依 Orchestrator 裁决以 #29 编号执行；#29 的 L147 与 #19 的 L12 均不在本区间）

## Patch #5
- 类型：替换
- 原行号：L280
- 原文行（完整一行，供 Merger 精确匹配）：- 若检测失败，自动降级为模式 C
- 新行（替换为完整新行；插入可多行，逐行列出）：- 若原判定为模式 B 或模式 D 且检测失败，自动降级为模式 C；若原判定为模式 A，维持模式 A
- 依据：审查报告 #5

## Patch #39
- 类型：替换
- 原行号：L284
- 原文行（完整一行，供 Merger 精确匹配）：- 模式 B → 模式 C 的降级条件与切换流程
- 新行（替换为完整新行；插入可多行，逐行列出）：- 模式 B → 模式 C、模式 D → 模式 C（或模式 A）的降级条件与切换流程
- 依据：审查报告 #39

## Patch #48
- 类型：替换
- 原行号：L290
- 原文行（完整一行，供 Merger 精确匹配）：Agent 确定运行方式后，必须在任务开始时输出：
- 新行（替换为完整新行；插入可多行，逐行列出）：Agent 确定运行方式后，必须在任务开始时输出（本条款自规约体系建成后的任务起生效，本次构建任务不适用）：
- 依据：审查报告 #48

## Patch #39
- 类型：替换
- 原行号：L297
- 原文行（完整一行，供 Merger 精确匹配）：- 降级情况：无 / 已从 B 降级至 C
- 新行（替换为完整新行；插入可多行，逐行列出）：- 降级情况：无 / 已从 B 降级至 C / 已从 D 降级至 C 或 A
- 依据：审查报告 #39

## Patch #35
- 类型：替换
- 原行号：L305
- 原文行（完整一行，供 Merger 精确匹配）：- 精简可读，30 秒抓住要点
- 新行（替换为完整新行；插入可多行，逐行列出）：- 精简可读：正文不超过 60 行，以列表与短句为主
- 依据：审查报告 #35

## Patch #36
- 类型：替换
- 原行号：L324
- 原文行（完整一行，供 Merger 精确匹配）：  - 可信度标签使用规范
- 新行（替换为完整新行；插入可多行，逐行列出）：  - 可信度标签一节包含第 1.4 节全部 5 个标签及含义，且 AGENTS.md 其余位置出现的标签拼写与用法与其一致
- 依据：审查报告 #36

## Patch #53
- 类型：插入
- 原行号：L325（锚点行，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：- 有问题则修正后重新审计
- 新行（替换为完整新行；插入可多行，逐行列出）：- 重新审计最多 3 轮；仍不通过的，输出未通过项清单与原因，标注 `[待补充]`，终止本轮校验并在最终汇报中置顶说明。
- 依据：审查报告 #53（锚点为『重新审计』行；L335 处同受该上限约束，不另出 Patch）

## Patch #15
- 类型：替换
- 原行号：L332
- 原文行（完整一行，供 Merger 精确匹配）：  - **`agent-execution-mode.md` 已列入索引且路径正确**
- 新行（替换为完整新行；插入可多行，逐行列出）：  - **`docs/agent-execution-mode.md` 已列入索引且路径正确**
- 依据：审查报告 #15

## Patch #37
- 类型：替换
- 原行号：L333
- 原文行（完整一行，供 Merger 精确匹配）：  - 标签/摘要与实际匹配
- 新行（替换为完整新行；插入可多行，逐行列出）：  - INDEX.md 每条记录的路径在 `docs/` 下实际存在；标题与文件一级标题一致；摘要不超过一句话；可信度状态为五个标签之一
- 依据：审查报告 #37

## Patch #42
- 类型：替换
- 原行号：L343
- 原文行（完整一行，供 Merger 精确匹配）：### 校验完成确认
- 新行（替换为完整新行；插入可多行，逐行列出）：### 校验完成确认（三步完成后输出）
- 依据：审查报告 #42

## Patch #15
- 类型：替换
- 原行号：L348
- 原文行（完整一行，供 Merger 精确匹配）：- docs/INDEX.md：N 篇文档，索引与实际文件一一对应，agent-execution-mode.md 已收录，可信度状态已填写
- 新行（替换为完整新行；插入可多行，逐行列出）：- docs/INDEX.md：N 篇文档，索引与实际文件一一对应，docs/agent-execution-mode.md 已收录，可信度状态已填写
- 依据：审查报告 #15

## Patch #11
- 类型：替换
- 原行号：L351
- 原文行（完整一行，供 Merger 精确匹配）：- 执行模式：多Agent协作 / 单Agent降级
- 新行（替换为完整新行；插入可多行，逐行列出）：- 协作模式：多 Agent 协作 / 单 Agent 降级
- 依据：审查报告 #11（依裁决本区间仅处理 L351，L359 归 #6）

## Patch #6
- 类型：替换
- 原行号：L359
- 原文行（完整一行，供 Merger 精确匹配）：1. **决策先行：** 首先完成第`〇`节（现状扫描 → 模式判定 → 协作检测 → 决策报告），报告后再动手。
- 新行（替换为完整新行；插入可多行，逐行列出）：1. **决策先行：** 首先完成第`〇`节（0.1 现状扫描 → 0.2 执行模式判定 → 0.4 输出决策报告，全程遵守 0.3 关键保护原则；协作能力检测按「权限与能力确认」一节执行），报告后再动手。
- 依据：审查报告 #6

## Patch #2
- 类型：替换
- 原行号：L362
- 原文行（完整一行，供 Merger 精确匹配）：   - 将结果写入 `_temp/verify-*.md` 和 `_temp/infer-*.md`
- 新行（替换为完整新行；插入可多行，逐行列出）：   - 将结果写入 `_temp/verify-*.md`（校验类）或 `_temp/infer-*.md`（推测类）
- 依据：审查报告 #2（依裁决本区间仅处理 L362；L119/L154/L162/L189 不在本区间）

## Patch #43
- 类型：替换
- 原行号：L364
- 原文行（完整一行，供 Merger 精确匹配）：3. **操作顺序（情况 A/B/C）：**
- 新行（替换为完整新行；插入可多行，逐行列出）：3. **操作顺序（仅情况 A/B/C；情况 D 按 0.4 直接执行第五节流程）：**
- 依据：审查报告 #43

## Patch #15+#16
- 类型：替换
- 原行号：L365
- 原文行（完整一行，供 Merger 精确匹配）：   - 执行校验与推测 → 处理 `docs/` 目录（保留/迁移/新建具体规约文档，**含 `agent-execution-mode.md`**）→ 创建/更新 `INDEX.md` → 创建/更新 `AGENTS.md` → 创建/更新 `README.md`
- 新行（替换为完整新行；插入可多行，逐行列出）：   - 执行校验与推测 → 处理 `docs/` 目录（保留/迁移/新建具体规约文档，**含 `docs/agent-execution-mode.md`**）→ 创建/更新 `docs/INDEX.md` → 创建/更新 `AGENTS.md` → 创建/更新 `README.md`
- 依据：审查报告 #15、#16（依 Orchestrator 裁决合并为一个 Patch）

## Patch #38
- 类型：替换
- 原行号：L367
- 原文行（完整一行，供 Merger 精确匹配）：5. **编码格式：** UTF-8，Markdown，标题层级规范
- 新行（替换为完整新行；插入可多行，逐行列出）：5. **编码格式：** UTF-8，Markdown，标题从二级起始、不跳级、单文件仅一个一级标题
- 依据：审查报告 #38

## Patch #50
- 类型：插入
- 原行号：L367（锚点行，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：5. **编码格式：** UTF-8，Markdown，标题层级规范
- 新行（替换为完整新行；插入可多行，逐行列出）：**Git 边界：** 除非用户明确要求，禁止执行 `git add/commit/push` 及分支操作；允许使用 `git log/diff` 等只读命令作为推测依据。
- 依据：审查报告 #50（依裁决与 #38 拆分为两个 Patch；锚点行同时被 Patch #38 替换，插入位置在替换后的行之后，条目编号顺延由 Merger 统一处理）
