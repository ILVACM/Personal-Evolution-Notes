# 维度 4.6 审查报告 —— 边界与例外

- 被审查文件：`prompts/Project-Spec-build.md`（共 381 行）
- 审查口径：是否明确「不做什么」（负面边界）；异常情况（权限不足、文件缺失、信息矛盾、推测失败）的处理路径是否完整；是否存在未定义行为的灰色地带。
- 说明：行号经 Grep 与结构化摘要交叉校准（Read 工具在 L134 之后存在 1 行偏移，已修正）。

## 问题清单

### 4.6-01
- 维度：4.6 边界与例外
- 严重度：❌ 不明确
- 行号+原文：L7「若任何一项缺失则立即报告而非静默跳过」；L11「确认其已就绪」；L14「若权限完备，直接开始执行。」
- 问题描述：权限/能力不足的异常只定义了「报告」动作，报告之后的行为未定义（停止等待？降级？放弃任务？）；MCP/Skills 未就绪也无后续路径。异常处理路径不完整。
- 修改建议：在第 12 行后补充：「报告后停止一切文件修改操作，等待用户确认后再继续；若仅多 Agent 协作能力缺失，无需等待，直接按第二节模式 B 降级执行，并在决策报告中注明。」

### 4.6-02
- 维度：4.6 边界与例外
- 严重度：❌ 不明确
- 行号+原文：L133「| `[待补充]` | 无任何线索，无法推测 | 需人工介入 |」
- 问题描述：推测失败的处理只写了「需人工介入」，但介入流程完全未定义：以什么形式请求介入、整体任务是否暂停、介入事项记录在哪里，均为空白。
- 修改建议：在第 133 行后补充：「遇 `[待补充]` 项时：不中断整体流程，将该项写入 `_temp/verify-*.md` 的『待人工介入』专节，继续完成其余工作；最终汇报中单列待介入清单，由用户决定补充方式。」

### 4.6-03
- 维度：4.6 边界与例外
- 严重度：❌ 不明确（信息矛盾导致灰色地带，与 4.2-22 同源）
- 行号+原文：L67「若判定为情况 D（仅校验修正），报告后直接进入第五节校验流程」 vs L74「无论……都必须执行一致性校验」
- 问题描述：提示词自身两条指令互相矛盾，情况 D 是否执行第一节校验未定义，属于规则内部冲突形成的灰色地带。
- 修改建议：将第 67 行改为「若判定为情况 D（仅校验修正），报告后仍须先执行第一节一致性校验（范围限定为已有规约文档与实际产物），通过后再进入第五节校验流程」。

### 4.6-04
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（负面边界缺失）
- 行号+原文：全文无「禁止修改代码/配置」条款；相关隐含依据仅为 L89「以实际代码/配置为准」
- 问题描述：校验逻辑默认只改文档、不改代码，但全文从未显式划定「本任务只动规约文档」的负面边界；执行者可能为消除差异而去修改代码、配置、依赖清单或 CI/CD 文件。
- 修改建议：在第 52 行后补充一条：「❌ 本任务仅允许新增/修改/移动/删除规约类文档（AGENTS.md、README.md、docs/、_temp/）；禁止修改任何代码、配置文件、依赖清单与 CI/CD 脚本。」

### 4.6-05
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（未定义行为）
- 行号+原文：L11 提及 Git 能力，全文无任何提交/推送条款
- 问题描述：变更完成后是否允许 git commit/push/建分支完全未定义；执行者可能擅自提交，也可能该留痕时不留痕，属灰色地带。
- 修改建议：在第六节执行要求（第 367 行后）补充一条：「**Git 边界：** 除非用户明确要求，禁止执行 `git add/commit/push` 及分支操作；允许使用 `git log/diff` 等只读命令作为推测依据。」

### 4.6-06
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（异常路径缺失）
- 行号+原文：L89「发现不一致时，以实际代码/配置为准」
- 问题描述：只定义了「文档 vs 实际」矛盾的处理；当实际产物之间互相矛盾（如 package.json 与 go.mod 并存、Makefile 与 CI 配置冲突）时以谁为准未定义。
- 修改建议：在第 89 行后补充：「若实际产物之间相互矛盾，以运行时实际生效者为准；无法判定生效关系时，将矛盾各方均标注 `[存在差异]`，矛盾对记入 `_temp/verify-discrepancies.md` 并列入待人工确认清单。」

### 4.6-07
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（冲突层级不全）
- 行号+原文：L207「**冲突处理：** `README.md` 与 `AGENTS.md` 或 `docs/` 冲突时，以 `AGENTS.md` 为最终权威」
- 问题描述：只覆盖 README 与规约的冲突；docs/ 同级文档之间互相矛盾时的裁决规则缺失（L197–L205 只规定各自「从属于 AGENTS.md」），属灰色地带。
- 修改建议：在第 207 行后补充：「docs/ 同级文档之间冲突时，一律以 AGENTS.md 表述为准；AGENTS.md 无表述的，冲突双方均标注 `[存在差异]` 并将矛盾对列入汇报等待用户裁定，不得擅自删除任何一方。」

### 4.6-08
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（未定义行为：可能死循环）
- 行号+原文：L325「有问题则修正后重新审计」；L335「不一致则修正」
- 问题描述：「修正—复检」循环无次数上限；遇到无法修正或反复不收敛的问题时行为未定义，可能无限循环。
- 修改建议：在第 325 行后补充：「重新审计最多 3 轮；仍不通过的，输出未通过项清单与原因，标注 `[待补充]`，终止本轮校验并在最终汇报中置顶说明。」（第 335 行处同样受此上限约束。）

### 4.6-09
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（异常路径缺失）
- 行号+原文：L368「**最终校验：** 操作完成后立即执行一次完整全局校验」
- 问题描述：最终校验不通过且无法修复时如何处理未定义（能否带病结束、如何汇报均空白）。
- 修改建议：在第 368 行后补充：「最终校验不通过的，按第五节修正后重跑；达到上一条 3 轮上限仍不通过的，允许带着未通过清单结束任务，但须在汇报中置顶说明并标注 `[待补充]`。」

### 4.6-10
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（未定义行为）
- 行号+原文：L119（写入 `_temp/verify-*.md`）；相关：L183–L193 标准状态含 `_temp/`、L205 称其为「临时参考」
- 问题描述：两处未定义行为：① `_temp/` 已存在历史 verify-*/infer-* 文件时覆盖、追加还是忽略未规定；② 任务结束时 `_temp/` 保留还是清理未规定（L205「临时参考」与 L183–L193 将其列入标准状态相互矛盾）。
- 修改建议：在第 121 行后补充：「写入前若已存在同名文件，直接覆盖（以本次结果为准）；任务结束后保留 `_temp/` 目录供审计追溯，不做清理。」

### 4.6-11
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（未定义行为）
- 行号+原文：L119「每个子 Agent 将结果写入 `_temp/verify-*.md`」
- 问题描述：多 Agent 并行写入时文件名唯一性无规则，两个子 Agent 写同一文件的冲突行为未定义。
- 修改建议：在第 119 行后补充：「Orchestrator 分派任务时为每个子任务指定唯一文件名（`verify-<模块名>.md` / `infer-<主题名>.md`）；禁止两个子 Agent 写入同一文件。」

### 4.6-12
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（未定义行为）
- 行号+原文：L56「在动手操作前，向我输出简要决策报告」；L67–L68「报告后……继续执行」；L381「输出决策报告后继续」
- 问题描述：决策报告输出后是等待用户确认还是自动继续，未显式声明；L381 暗示自动继续但无「无需等待」的明确表述，两种理解均可能。
- 修改建议：将第 56 行改为「在动手操作前，向我输出简要决策报告：无需等待确认，输出后立即按 L67/L68 对应路径继续执行；若用户另行指示，以用户指示为准。」

### 4.6-13
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（灰色地带）
- 行号+原文：L29（扫描对象含 `CONTRIBUTING.md` 等散落文档）；L251「已有文档应被合理归入此目录（移动/重命名），而非丢弃」
- 问题描述：根目录约定俗成的文档（LICENSE、CHANGELOG、CONTRIBUTING 等）是否必须移入 docs/ 未定义：移动违反仓库惯例，不移动违反 L251，执行者两难。
- 修改建议：在第 251 行后补充：「按惯例保留在根目录的文档（LICENSE、CHANGELOG、CONTRIBUTING 等）可原地保留、不纳入 `docs/INDEX.md`，但须在决策报告『现状』中列明。」

### 4.6-14
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（极端场景缺失）
- 行号+原文：L95–L112（1.2 推测边界）
- 问题描述：仓库完全无代码、无线索的极端场景未定义：全部信息点均为 `[待补充]` 并触发人工介入时，是否仍须产出规约骨架未规定，任务可能悬置。
- 修改建议：在第 112 行后补充：「若项目无任何代码与文档线索，仍须创建 `AGENTS.md`、`docs/INDEX.md`、`docs/agent-execution-mode.md` 骨架，信息点全部标注 `[待补充]`，并在汇报中输出待人工补充清单。」

### 4.6-15
- 维度：4.6 边界与例外
- 严重度：⚠️ 有歧义（未定义行为）
- 行号+原文：L246–L251（docs/ 目录要求）
- 问题描述：docs/ 内存在非规约类文件（图片、设计稿、会议纪要等）时，保留、移动还是索引均无规则。
- 修改建议：在第 251 行后补充：「docs/ 下的非规约类文件（图片、设计稿、会议纪要等）原地保留或移入 `docs/assets/` 等子目录，不纳入 INDEX.md，并在汇报中说明处理方式。」

### 4.6-16
- 维度：4.6 边界与例外
- 严重度：✅ 明确
- 行号+原文：L47–L52（关键保护原则）、L103–L109（禁止推测红线）、L145/L175（禁止一次性加载整个规约）、L121（不支持多 Agent 的降级路径）、L280（检测失败自动降级为模式 C）、L89（文档与实际不一致以实际为准）
- 问题描述：上述负面边界与异常路径定义清晰，无需修改。
- 修改建议：无。

## 总判定

**维度 4.6 总判定：❌ 不完整（边界与例外处理路径存在多处缺口，须补齐后方可执行）。** 负面边界在保护原则（L47–L52）、推测红线（L103–L109）、禁止全量加载（L145/L175）处清晰，环境不支持的降级路径（L121/L280）完整；但权限不足报告后的行为、推测失败后的人工介入流程、校验循环上限、最终校验失败路径四类异常处理不完整（含 3 处 ❌），且情况 D 执行路径、是否可修改代码、Git 操作、`_temp/` 存留构成多处未定义行为的灰色地带。
