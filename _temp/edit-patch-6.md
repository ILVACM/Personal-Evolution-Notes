# Edit Patch 6（Orchestrator 补发 · 裁决 #2）

- 来源：Merger 合并日志「交叉引用有效性」报告的 2 处因本次修改自身导致明显失效的引用，依修改原则 3.3「若因修改导致引用失效，同步修正引用目标」补发
- 覆盖指令：#57（行号漂移的根因）、#1（模式改名的根因）
- 说明：仅修正引用目标，不改写其他文字

## Patch #57-ref
- 类型：替换
- 锚点行（修订稿新 L60，完整一行，供 Merger/Validator 精确匹配）：在动手操作前，向我输出简要决策报告：无需等待确认，输出后立即按 L67/L68 对应路径继续执行；若用户另行指示，以用户指示为准。
- 新行：在动手操作前，向我输出简要决策报告：无需等待确认，输出后立即按下方两条对应路径继续执行；若用户另行指示，以用户指示为准。
- 依据：审查报告 #57（该句为 #57 修改产物；原「L67/L68」行号引用因本次插入修改漂移失效，改为位置无关表述「下方两条」，指向紧随其后的两条情况路径）

## Patch #1-ref
- 类型：替换
- 锚点行（修订稿新 L13，完整一行，供精确匹配）：  报告后停止一切文件修改操作，等待用户确认后再继续；若仅多 Agent 协作能力缺失，无需等待，直接按第二节模式 B 降级执行，并在决策报告中注明。
- 新行：  报告后停止一切文件修改操作，等待用户确认后再继续；若仅多 Agent 协作能力缺失，无需等待，直接按第二节「构建协作方式二：单 Agent（降级）」执行，并在决策报告中注明。
- 依据：审查报告 #1（「模式 B」已由 #1 改名，旧称引用失效，同步替换为新名称）
