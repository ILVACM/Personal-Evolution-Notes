# SpecChecker 审查报告：规约体系组成表述清晰度

- 被审查文件：`d:\Personal-PitFall-Log\prompts\Project-Spec-build.md`（381 行）
- 审查维度：「规约体系组成」表述是否清晰无歧义（只审表述，不审业务逻辑）
- 审查基准：规约体系 = AGENTS.md + docs/ 目录；路由链路 AGENTS.md → docs/INDEX.md → 目标细则；约束层级 AGENTS.md > docs/ 细则 > README.md（无约束力）

## 五个要点逐项判定

| 要点 | 判定 |
|------|------|
| ① 是否显式写出组成结构（规约体系 = AGENTS.md + docs/） | ⚠️ 有歧义（只能推断，无定义句，且树状图混入非体系成员） |
| ② AGENTS.md 与 docs/ 的关系是否一句话可概括 | ⚠️ 有歧义（分散于树状图注释、职能表、冲突处理三处，需拼凑） |
| ③ docs/INDEX.md 的路由入口角色是否明确 | ✅ 明确（三处独立表述，未被淹没） |
| ④ 「AGENTS.md 中给出路由指向」是否被清晰表述 | ✅ 明确（4.1 板块 + 第五节校验项双向闭环） |
| ⑤ 是否存在误导（docs/ 可选 / README.md 有约束力） | ⚠️ 有歧义（README 侧三重锁定无风险；INDEX.md 缺「必建」标注，存在轻微误读空间） |

## 问题明细

### 问题 1（对应要点①）｜⚠️ 有歧义

- **行号+原文**：L179–L194「## 三、规约体系标准（最终状态要求）……├── AGENTS.md # 规约总纲 + 规约路由入口（面向 Agent）/ ├── README.md / ├── _temp/ / └── docs/ # 规约细则目录」；L38「不存在 `AGENTS.md`，不存在 `docs/` 目录，无有效规约体系」
- **问题描述**：全文没有一句「规约体系 = AGENTS.md + docs/ 目录」的显式定义。组成只能从 L38 的否定式判定标准反向推断；且第三节树状图将不属于规约体系的 README.md（无约束力）与 _temp/（临时产物）与体系成员并列呈现，标题又是「规约体系标准」，读者需自行区分哪些在体系内、哪些在体系外。
- **修改建议**：在 L181（「无论执行路径如何，最终必须达到以下标准状态：」）后补充一句：「**规约体系由 `AGENTS.md` 与 `docs/` 目录两部分组成：AGENTS.md 为规约总纲，docs/ 为规约细则目录；`README.md` 与 `_temp/` 为仓库配套/临时文件，不属于规约体系。**」

### 问题 2（对应要点②）｜⚠️ 有歧义

- **行号+原文**：L185「AGENTS.md # 规约总纲 + 规约路由入口」与 L190「docs/ # 规约细则目录」；L200–L203 职能表「约束力」列（最高约束 / 从属于 `AGENTS.md`）；L207「冲突处理：……以 `AGENTS.md` 为最终权威」
- **问题描述**：AGENTS.md（总纲、最高权威、路由出发点）与 docs/（细则、从属）的关系被拆散在三处——树状图注释、职能表「约束力」列、冲突处理条款，没有任何一句话直接概括，读者必须交叉拼凑才能得出完整关系，且「AGENTS.md 是 Agent 第一入口」这一层也仅由 L185「面向 Agent」间接带出。
- **修改建议**：在 L196（「### 各部分职能定位」标题）后、职能表前补充一句：「**一句话概括：AGENTS.md 是规约总纲与 Agent 第一入口（全局最高权威），其内给出指向 docs/ 目录的路由指引；docs/ 存放从属于 AGENTS.md 的规约细则，路由链路为 AGENTS.md → docs/INDEX.md → 目标细则文档。**」

### 问题 3（对应要点③）｜✅ 明确

- **行号+原文**：L191「INDEX.md # 索引文档（智能路由入口）」；L201「规约索引与路由；按需跳转，禁止全量加载」；L224–L225「明确索引路径：`docs/INDEX.md`；标准流程：`AGENTS.md → docs/INDEX.md → 目标文档`」
- **问题描述**：INDEX.md 的智能路由入口角色在树状图、职能表、AGENTS.md 必备板块「规约路由指引」三处均有独立且一致的表述，未被淹没在文件列表中。
- **修改建议**：无需修改。可选增强：将 L191「`├── INDEX.md           # 索引文档（智能路由入口）`」改为「`├── INDEX.md           # 索引与路由文档（docs/ 的智能路由入口）`」，补明入口归属，避免与 L185 AGENTS.md 的「规约路由入口」字面并立产生「两个入口」的轻微困惑。

### 问题 4（对应要点④）｜✅ 明确

- **行号+原文**：L185「规约路由入口」；L220「路由路径：`→ docs/agent-execution-mode.md`」；L223–L225「规约路由指引：明确索引路径 `docs/INDEX.md`；标准流程 `AGENTS.md → docs/INDEX.md → 目标文档`」；L321「路由路径正确（指向 `docs/INDEX.md`、`docs/agent-execution-mode.md` 等）」
- **问题描述**：「AGENTS.md 中给出路由指向」这一事实表述清晰：第三节树注声明角色，4.1 以「必备板块」形式要求 AGENTS.md 写出两条路由路径及标准流程，第五节 Step 1 又将其列为校验项，表述与校验形成闭环。
- **修改建议**：无需修改。

### 问题 5（对应要点⑤）｜⚠️ 有歧义

- **行号+原文**：L191–L192「├── INDEX.md # 索引文档（智能路由入口）／├── agent-execution-mode.md # Agent 运行方式判定标准（必建）」；L239–L244（4.2 节全文）；README 侧对照 L204「无约束力」、L207、L302「纯展示，无约束条款」
- **问题描述**：
  a) README.md 侧无误导风险：L204「无约束职能｜无约束力」、L207 冲突以 AGENTS.md 为准、L302「纯展示，无约束条款」三重锁定，不存在让读者误以为 README.md 有约束力的表述。
  b) docs/ 整体必建性由 L181「无论执行路径如何，最终必须达到以下标准状态」兜底，基本无「可选」误读；但树状图中同级兄弟 agent-execution-mode.md 标注了「（必建）」，INDEX.md 却无任何强制标注，4.2 节（L239–L244）也未出现「必建/不可省略」字样，标注不对称，读者可能误以为 INDEX.md（进而 docs/ 的索引层）可以省略。
- **修改建议**：将 L191「`├── INDEX.md           # 索引文档（智能路由入口）`」改为「`├── INDEX.md           # 索引与路由文档（docs/ 的智能路由入口，必建）`」；并在 L240（4.2 节标题行 L239 之后）补充一句：「**此文档为规约体系的必建交付物，构建时必须生成，不可省略。**」

## 该维度总判定

**⚠️ 基本清晰，存在三处需修补的表述缺口。** 路由链路、INDEX.md 路由入口角色、AGENTS.md 路由指引职责、README.md 无约束力均表述明确且有校验闭环；但全文缺少「规约体系 = AGENTS.md + docs/ 目录」的显式定义句，AGENTS.md 与 docs/ 的总纲/细则关系需跨三处拼凑，INDEX.md 缺少与 agent-execution-mode.md 对称的「必建」标注，建议按上述三处修改建议补齐。
