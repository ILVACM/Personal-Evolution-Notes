# 编辑 Patch 批次 1（原文 L1–L138）

- 目标文件：d:\Personal-PitFall-Log\prompts\Project-Spec-build.md
- 负责区间：原文 L1–L138
- Patch 数量：25
- 覆盖指令：#2 #3 #4 #7 #9 #10 #12 #13 #14 #17 #18 #20 #21 #22 #23 #24 #25 #26 #27 #28 #49 #55 #56 #57 #59

说明（供 Merger）：
1. 按 Orchestrator 裁决 1：指令 #12 中针对 L74 的「推断→推测」改动已被 #4 整句替换吸收，跳过；L74 仅应用 #4。
2. 按 Orchestrator 裁决 2：L119 共两个 Patch——#55+#56（插入，锚点 L119，先 #55 文本后 #56 文本）与 #2（替换）。两者针对同一原文行，建议先应用插入、再应用替换（替换不影响已插入的新行）。
3. L11（#18+#24）、L80（#18+#25）、L67（#3+#21）、L40（#20+#14）、L41（#14+#21）均为同一行内两条指令的不重叠短语改动，参照裁决 3/4 的合并精神各合并为一个 Patch，短语按其在原句中的先后顺序依次套用。
4. Patch #49 的插入按指令措辞「补充一条」作为新列表项（❌ 子句，与前文 ❌/✅ 项并列）；其余插入均为锚点行所在列表项的延续行（行首缩进 2 空格）或同段落延续行。
5. 所有新行文本逐字取自指令建议原文（引号内内容），未做改写。

---

## Patch #14
- 类型：替换
- 原行号：3
- 原文行（完整一行，供 Merger 精确匹配）：
```
你的任务是确保本项目仓库具备一套符合下述规范的 **Agent 规约**。这不意味着必须从零创建——你需要先评估现状，再决定执行路径。最终目标是：无论起点如何，结束时 Agent 规约必须完全符合本提示词定义的标准。
```
- 新行（替换为完整新行）：
```
你的任务是确保本项目仓库具备一套符合本提示词下述标准的 **Agent 规约**。这不意味着必须从零创建——你需要先评估现状，再决定执行路径。最终目标是：无论起点如何，结束时 Agent 规约必须完全符合本提示词定义的标准。
```
- 依据：审查报告 #14

## Patch #18+#24
- 类型：替换
- 原行号：11
- 原文行（完整一行，供 Merger 精确匹配）：
```
- 若项目依赖 MCP 服务或特定 Skills（如 Git 操作等），确认其已就绪
```
- 新行（替换为完整新行；先套用 #18 的「依赖 MCP 服务」改动，再套用 #24 的「确认其已就绪」改动）：
```
- 若项目依赖 MCP（Model Context Protocol）服务或特定 Skills（如 Git 操作等），以实际调用一次最小操作验证就绪（如成功执行一次只读命令 `git status`）；调用失败即视为缺失，按上一条『立即报告』处理
```
- 依据：审查报告 #18、#24

## Patch #9
- 类型：插入
- 原行号：12（锚点行号，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：
```
- 多 Agent 协作能力检测：检测当前环境是否支持多 Agent 协作（子 Agent 派发 / Worker / Task Delegation 等）
```
- 新行（插入 1 行，作为该列表项的延续行，行首缩进 2 空格）：
```
  报告后停止一切文件修改操作，等待用户确认后再继续；若仅多 Agent 协作能力缺失，无需等待，直接按第二节模式 B 降级执行，并在决策报告中注明。
```
- 依据：审查报告 #9

## Patch #7
- 类型：插入
- 原行号：24（锚点行号，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：
```
扫描项目根目录及子目录，重点关注：
```
- 新行（插入 1 行，作为同段落延续行）：
```
递归扫描全部目录；扫描完成的判定标准为下述 5 个问题均能给出答案（因权限或读取失败无法回答的，记为『无法回答 + 原因』）。扫描结论直接写入决策报告的『现状』字段。
```
- 依据：审查报告 #7

## Patch #14
- 类型：替换
- 原行号：38
- 原文行（完整一行，供 Merger 精确匹配）：
```
| A. 从零新建 | 不存在 `AGENTS.md`，不存在 `docs/` 目录，无有效规约体系 | 按规范完整创建 |
```
- 新行（替换为完整新行）：
```
| A. 从零新建 | 不存在 `AGENTS.md`，不存在 `docs/` 目录，无有效规约体系 | 按本提示词完整创建规约 |
```
- 依据：审查报告 #14

## Patch #20+#14
- 类型：替换
- 原行号：40
- 原文行（完整一行，供 Merger 精确匹配）：
```
| C. 重构规范化 | 已有一定规模的规约文档，但结构/格式/索引不符合本规范 | 保留核心内容，重组结构，补建索引，统一格式 |
```
- 新行（替换为完整新行；按 Orchestrator 裁决 3：#20 采用含量化阈值版本，与 #14 的「按规范→按本提示词」改动在同一句按原文顺序合并）：
```
| C. 重构规范化 | 已有规约类文档（AGENTS.md 或 docs/ 下的 .md 文件）合计 ≥2 篇且总行数 ≥100 行，但结构/格式/索引不符合本提示词标准 | 保留核心内容，重组结构，补建索引，统一格式 |
```
- 依据：审查报告 #20、#14

## Patch #14+#21
- 类型：替换
- 原行号：41
- 原文行（完整一行，供 Merger 精确匹配）：
```
| D. 校验修正 | 已有完整规约且基本符合规范，仅存在小问题 | 仅修正不一致处，不做大改动 |
```
- 新行（替换为完整新行；按 Orchestrator 裁决 4：#14 与 #21 短语不重叠，按原文顺序合并）：
```
| D. 校验修正 | 已有完整规约且基本符合本提示词标准，仅存在不影响 Agent 解析与执行的问题（限：格式不一致、索引个别条目缺失、个别标签过时三类；不含板块缺失与路由失效） | 仅修正不一致处，不做大改动 |
```
- 依据：审查报告 #14、#21

## Patch #22
- 类型：替换
- 原行号：50
- 原文行（完整一行，供 Merger 精确匹配）：
```
- ✅ 若某份已有文档的内容确实与当前项目不符（如过时、错误），应标注 `[已过时-待确认]` 而非直接删除，并在汇报中说明
```
- 新行（替换为完整新行）：
```
- ✅ 若某份已有文档的内容确实与当前项目不符（如过时、错误），必须标注 `[已过时-待确认]` 而非直接删除，并在汇报中说明
```
- 依据：审查报告 #22

## Patch #23
- 类型：插入
- 原行号：51（锚点行号，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：
```
- ✅ 若需合并多份文档，合并后原文件可删除，但必须确保所有内容已被完整迁移
```
- 新行（插入 1 行，作为该列表项的延续行，行首缩进 2 空格）：
```
  『完整迁移』以逐项比对判定：原文件的标题与关键信息点须全部出现在合并后的目标文件中，或已标注 `[已过时-待确认]`；比对通过并记入汇报后方可删除原文件。
```
- 依据：审查报告 #23

## Patch #49
- 类型：插入
- 原行号：52（锚点行号，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：
```
- ✅ 重构时优先移动和重命名，而非删除后重写
```
- 新行（插入 1 行；指令措辞为「补充一条」，作为新列表项与前文 ❌/✅ 项并列，保持列表语境）：
```
- ❌ 本任务仅允许新增/修改/移动/删除规约类文档（AGENTS.md、README.md、docs/、_temp/）；禁止修改任何代码、配置文件、依赖清单与 CI/CD 脚本。
```
- 依据：审查报告 #49

## Patch #57
- 类型：替换
- 原行号：56
- 原文行（完整一行，供 Merger 精确匹配）：
```
在动手操作前，向我输出简要决策报告：
```
- 新行（替换为完整新行）：
```
在动手操作前，向我输出简要决策报告：无需等待确认，输出后立即按 L67/L68 对应路径继续执行；若用户另行指示，以用户指示为准。
```
- 依据：审查报告 #57

## Patch #3+#21
- 类型：替换
- 原行号：67
- 原文行（完整一行，供 Merger 精确匹配）：
```
- 若判定为情况 D（仅校验修正），报告后直接进入第五节校验流程，无需大动。
```
- 新行（替换为完整新行；#3 与 #21 短语不重叠，按原文顺序合并：先 #3 的「直接进入第五节校验流程」改动，再 #21 的「无需大动」改动）：
```
- 若判定为情况 D（仅校验修正），报告后仍须先执行第一节一致性校验（范围限定为已有规约文档与实际产物），通过后再进入第五节校验流程，不执行第三、四节的新建与重组操作。
```
- 依据：审查报告 #3、#21

## Patch #4
- 类型：替换
- 原行号：74
- 原文行（完整一行，供 Merger 精确匹配）：
```
⚠️ 本节为强制执行环节。无论 README 或现有文档看起来多么完整可信，都必须执行一致性校验；无论文档是否缺失，都必须在受限范围内进行自适应推断。此环节是规约可信度的基石。
```
- 新行（替换为完整新行；按 Orchestrator 裁决 1：整句替换，#12 对 L74 的改动已被吸收）：
```
⚠️ 本节为强制执行环节。无论 README 或现有文档看起来多么完整可信，都必须执行一致性校验；当文档缺失或不完整时，必须在受限范围内进行自适应推测（边界条件见 1.2 节）。此环节是规约可信度的基石。
```
- 依据：审查报告 #4

## Patch #18+#25
- 类型：替换
- 原行号：80
- 原文行（完整一行，供 Merger 精确匹配）：
```
**校验对象：** 目录结构、入口文件、配置文件（package.json/pyproject.toml/go.mod 等）、核心代码模块、CI/CD 配置。
```
- 新行（替换为完整新行；#25 与 #18 短语不重叠，按原文顺序合并：先 #25 的「核心代码模块」改动，再 #18 的「CI/CD 配置」改动）：
```
**校验对象：** 目录结构、入口文件、配置文件（package.json/pyproject.toml/go.mod 等）、入口文件及其直接导入/引用的模块（无法判定依赖关系时，以入口文件所在目录为范围）、CI/CD（持续集成/持续部署）配置。
```
- 依据：审查报告 #25、#18

## Patch #26
- 类型：替换
- 原行号：91
- 原文行（完整一行，供 Merger 精确匹配）：
```
**不可跳过：** 即使文档声称"最新"，也必须执行至少一次自动化交叉比对。
```
- 新行（替换为完整新行）：
```
**不可跳过：** 即使文档声称"最新"，也必须完成至少一轮 『文档声明 → 实际产物』逐项比对：比对须以实际读取配置文件、目录列表、脚本为准（不得仅凭文档文本判断），逐项比对结果须写入 `_temp/verify-*.md`。
```
- 依据：审查报告 #26

## Patch #27
- 类型：插入
- 原行号：95（锚点行号，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：
```
当关键信息无文档说明或说明不完整时，允许基于有限线索进行推测，但必须严格遵守以下边界：
```
- 新行（插入 1 行，作为同段落延续行）：
```
此处『关键信息』指按第三、四节构建规约所必需的信息：项目用途、技术栈、运行环境与部署目标、目录结构、入口与构建命令。
```
- 依据：审查报告 #27

## Patch #59
- 类型：插入
- 原行号：112（锚点行号，新内容加在该行之后）
- 原文行（完整一行，供 Merger 精确匹配）：
```
**标注要求：** 所有推测内容必须显式标注 `[推测]` 或 `[推断]`，并注明依据来源（如"基于 package.json dependencies 推断"）。
```
- 新行（插入 1 行，作为同段落延续行）：
```
若项目无任何代码与文档线索，仍须创建 `AGENTS.md`、`docs/INDEX.md`、`docs/agent-execution-mode.md` 骨架，信息点全部标注 `[待补充]`，并在汇报中输出待人工补充清单。
```
- 依据：审查报告 #59

## Patch #17
- 类型：替换
- 原行号：118
- 原文行（完整一行，供 Merger 精确匹配）：
```
- **任务拆分：** Orchestrator 将校验/推测任务按模块/目录拆分为独立子任务。
```
- 新行（替换为完整新行）：
```
- **任务拆分：** Orchestrator（主控）将校验/推测任务按模块/目录拆分为独立子任务。
```
- 依据：审查报告 #17

## Patch #55+#56
- 类型：插入
- 原行号：119（锚点行号，新内容加在该行之后；按 Orchestrator 裁决 2：#55 与 #56 合并为一个 Patch，先 #55 文本后 #56 文本）
- 原文行（完整一行，供 Merger 精确匹配）：
```
- **临时文件机制：** 每个子 Agent 将结果写入 `_temp/verify-*.md`（如 `_temp/verify-backend-deps.md`、`_temp/infer-tech-stack.md`），禁止直接在内存中返回长文本。
```
- 新行（插入 2 行，作为该列表项的延续行，行首缩进 2 空格；第 1 行为 #55 文本，第 2 行为 #56 文本）：
```
  写入前若已存在同名文件，直接覆盖（以本次结果为准）；任务结束后保留 `_temp/` 目录供审计追溯，不做清理。
  Orchestrator 分派任务时为每个子任务指定唯一文件名（`verify-<模块名>.md` / `infer-<主题名>.md`）；禁止两个子 Agent 写入同一文件。
```
- 依据：审查报告 #55、#56

## Patch #2
- 类型：替换
- 原行号：119
- 原文行（完整一行，供 Merger 精确匹配；与上一 Patch #55+#56 同锚一行，建议先应用插入再应用本替换）：
```
- **临时文件机制：** 每个子 Agent 将结果写入 `_temp/verify-*.md`（如 `_temp/verify-backend-deps.md`、`_temp/infer-tech-stack.md`），禁止直接在内存中返回长文本。
```
- 新行（替换为完整新行）：
```
- **临时文件机制：** 每个子 Agent 将结果写入 `_temp/verify-*.md`（校验类）或 `_temp/infer-*.md`（推测类）（如 `_temp/verify-backend-deps.md`、`_temp/infer-tech-stack.md`），禁止直接在内存中返回长文本。
```
- 依据：审查报告 #2

## Patch #13
- 类型：替换
- 原行号：120
- 原文行（完整一行，供 Merger 精确匹配）：
```
- **汇总机制：** 主 Agent 仅读取 `_temp/` 下的结果文件进行汇总，生成最终的可信度分层报告。
```
- 新行（替换为完整新行）：
```
- **汇总机制：** Orchestrator（主控）仅读取 `_temp/` 下的结果文件进行汇总，生成最终的可信度分层报告。
```
- 依据：审查报告 #13

## Patch #28
- 类型：替换
- 原行号：125
- 原文行（完整一行，供 Merger 精确匹配）：
```
在项目背景、技术栈、架构概述等关键输出段落中，必须使用以下标签之一作为前缀，便于人工审阅时快速判断复核力度：
```
- 新行（替换为完整新行）：
```
在项目背景、技术栈、架构概述、运行环境、部署目标五类段落，以及决策报告与校验报告中的结论性语句中，必须使用以下标签之一作为前缀，便于人工审阅时快速判断复核力度：
```
- 依据：审查报告 #28

## Patch #12
- 类型：替换
- 原行号：131
- 原文行（完整一行，供 Merger 精确匹配）：
```
| `[推测]` | 无直接文档，基于客观线索推断 | 中优先级确认 |
```
- 新行（替换为完整新行）：
```
| `[推测]` | 无直接文档，基于单一客观线索推测得出 | 中优先级确认 |
```
- 依据：审查报告 #12

## Patch #12
- 类型：替换
- 原行号：132
- 原文行（完整一行，供 Merger 精确匹配）：
```
| `[推断]` | 基于多处间接证据综合得出 | 中优先级确认 |
```
- 新行（替换为完整新行）：
```
| `[推断]` | 基于多处间接证据综合推断得出（线索数量 ≥ 2 且相互印证） | 中优先级确认 |
```
- 依据：审查报告 #12

## Patch #10
- 类型：插入
- 原行号：133（锚点行号，新内容加在该行之后；该行位于表格末行，新行作为表格后的独立段落，不破坏表格结构）
- 原文行（完整一行，供 Merger 精确匹配）：
```
| `[待补充]` | 无任何线索，无法推测 | 需人工介入 |
```
- 新行（插入 1 行）：
```
遇 `[待补充]` 项时：不中断整体流程，将该项写入 `_temp/verify-*.md` 的『待人工介入』专节，继续完成其余工作；最终汇报中单列待介入清单，由用户决定补充方式。
```
- 依据：审查报告 #10
