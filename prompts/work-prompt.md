读取分析上面这段提示词，说明我的任务需求（确保理解正确符合预期），并进行修改优化随后完整输出（便于我直接复制粘贴）。注意提示词用于命令引导本地agent工具执行操作，保证agent已经具备所有必要的权限和能力（如文件读写、相关skills技能能力、或者MCP服务辅助等）。另外要求，如果你分析时认为任务要求有超出单个agent能力上限的隐患（假设本地agent所使用实际模型上下文为256K），则自适应在提示词中添加要求本地agent使用多agent协作模式来完成任务（变相扩展上下文并提高工作能力），确保本地agent支持多agent协作模式。

先说明我的任务需求（确保理解正确符合预期），再具体给我设计提示词。特别注意提示词用于命令引导本地agent工具执行操作，保证agent已经具备所有必要的权限和能力（如文件读写、相关skills技能能力、或者MCP服务辅助等）。另外要求，如果你分析时认为任务要求有超出单个agent能力上限的隐患（假设本地agent所使用实际模型上下文为256K），则自适应在提示词中添加要求本地agent使用多agent协作模式来完成任务（变相扩展上下文并提高工作能力），确保本地agent支持多agent协作模式。
